#include <stdio.h>

int main(){

char bisiesto(short int anio);
    printf("Ingrese el año: ");
    scanf()
{
    if (anio%4==0)
{
    if(anio%100==0)
    {
        if(anio%400==0)
        {
            printf("si es bisiesto");
            return anio;
        }
        else 
        {
            printf("no es bisiesto");
            return anio;
            
        }
        
    }
}
else
{
    printf( "no es bisiesto");
    return anio;
    
}
}

